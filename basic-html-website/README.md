# HTML website

## Purpose

To learn how to properly semantically structure a website. And to build a website with semantic structure to be styled with CSS later.

## Requirements

- [x] Semantic HTML structure
- [x] SEO meta tags in header
- [x] Navigation bar on all pages
- [x] Contact page with form with appropiate fields
